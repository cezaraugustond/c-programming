#include <stdio.h>

int main() {
    char letra;
    int idade;
    float altura;
    double pi;
    scanf("%c", &letra);
    scanf("%d", &idade);
    scanf("%f", &altura);
    scanf("%f", &pi);
    return 0;
}
