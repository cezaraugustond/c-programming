#include <stdio.h>

int main() {
    int numero1;
    float numero2;
    printf("Digite dois numeros: ");
    scanf("%d%f", &numero1, &numero2);
    printf("Numeros: %d e %f \n", numero1, numero2);
    return 0;
}
