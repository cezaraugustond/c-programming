#include <stdio.h>

int main() {
    int x = 5, y, z;
    y = x;
    z = y + 10;
    printf("Valor de x = %d \n", x);
    printf("Valor de y = %d \n", y);
    printf("Valor de z = %d \n", z);
    return 0;
}
